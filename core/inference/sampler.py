"""Sampling controls placeholder (temperature, top-k, top-p)."""

from typing import Any


def apply_sampling_controls(logits: Any, temperature: float = 1.0) -> Any:
    _ = temperature
    return logits
